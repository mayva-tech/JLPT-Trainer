/**
 * @deprecated Prefer importing from QuestRunner.
 * Thin re-export so older imports keep working.
 */
export {
  QuestRunner,
  QuestRunner as CityHallQuestRunner,
  type QuestRunOutcome,
} from "./QuestRunner";
